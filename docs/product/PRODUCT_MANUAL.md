# 产品使用手册 (Product Manual)

## 产品概述

**Fotopro Axis - Annual Strategic Command Center** 是一个企业级战略执行管理系统，帮助团队从战略规划到任务执行的全流程管理。

### 核心价值

- 📊 **四层架构管理**: 从战略目标（L1）到具体任务（L4）的完整体系
- 📅 **可视化时间轴**: 甘特图展示项目进度和时间安排
- 🤖 **AI 智能辅助**: 自动生成任务建议和周报内容
- 📈 **数据驱动决策**: 实时统计和进度追踪
- 🔍 **多维度筛选**: 按负责人、渠道、产品、标签、时间筛选
- 📤 **数据导出**: 一键导出完整报表（CSV 格式）

## 快速开始

### 1. 登录系统

**默认账户**:
- **Admin**: 用户名 `admin`，密码 `123`（完整权限）
- **User**: 用户名 `user`，密码 `123`（普通用户）
- **Viewer**: 用户名 `viewer`，密码 `123`（只读权限）

### 2. 了解界面布局

```
┌─────────────────────────────────────────────────┐
│  顶部导航栏：当前策略信息、统计、操作按钮        │
├──────────┬──────────────────────────────────────┤
│          │                                       │
│  侧边栏  │         主内容区                      │
│  策略树  │  - 筛选面板                           │
│  (L1-L3) │  - 甘特图                            │
│          │  - 任务列表                           │
│          │                                       │
└──────────┴──────────────────────────────────────┘
```

## 核心功能详解

### 一、策略管理 (Strategy Management)

#### 1.1 创建策略节点

**操作步骤**:
1. 在侧边栏选择父级策略节点（L1 无需父级）
2. 点击右上角 "+" 按钮或右键菜单
3. 填写策略信息：
   - **策略名称**: 必填，描述策略目标
   - **负责人**: 策略负责人
   - **渠道/产品**: 业务维度分类
   - **标签**: 使用逗号分隔，支持快捷选择
   - **时间周期**: 开始和结束日期
   - **KPI 指标**: 可添加多个关键指标
4. 点击 "Save Strategy" 保存

**层级说明**:
- **L1 (Level 1)**: 顶级战略目标，如"2026 全球品牌心智工程"
- **L2 (Level 2)**: 二级策略，如"北美市场扩张"
- **L3 (Level 3)**: 三级策略，如"洛杉矶旗舰店落地"

#### 1.2 编辑策略

1. 在侧边栏点击策略节点
2. 点击右上角编辑按钮
3. 修改信息后保存

#### 1.3 删除策略

⚠️ **警告**: 删除策略会同时删除所有子策略和关联任务，操作不可撤销。

1. 选择要删除的策略节点
2. 点击右上角删除按钮
3. 确认删除操作

### 二、任务管理 (Task Management)

#### 2.1 创建任务 (L4)

**操作步骤**:
1. 选择 L3 策略节点（只有 L3 可以创建任务）
2. 点击右上角 "AI 辅助生成" 或手动添加
3. 填写任务信息：
   - **任务内容**: 具体执行事项
   - **状态**: 准备/进行中/已完成/已确认
   - **进度**: 0-100% 完成度
   - **优先级**: P0（紧急）/P1（高）/P2（普通）
   - **负责人**: 执行人
   - **时间**: 开始和结束日期

#### 2.2 编辑任务详情

1. 在任务列表中点击任务
2. 在弹窗中编辑：
   - 基本信息（内容、状态、进度）
   - 审核评分（审核人、评分、评语）
   - 任务备注
   - **执行汇报**（重要功能）

#### 2.3 任务汇报 (Task Report)

**汇报类型**:
- **计划**: 任务计划说明
- **进展**: 执行进展情况
- **问题**: 遇到的问题和风险
- **结果**: 完成结果和成果
- **复盘**: 经验总结和反思

**添加汇报**:
1. 打开任务编辑弹窗
2. 在"执行汇报"区域点击 "Add Report"
3. 选择汇报类型
4. 填写汇报内容
5. 保存任务

**重要**: 每个汇报都会作为独立记录保存，是数据导出的最小颗粒度。

### 三、筛选和视图 (Filtering & Views)

#### 3.1 多维度筛选

在筛选面板中可以按以下维度筛选：

- **负责人 (Owner)**: 按执行人筛选
- **渠道 (Channel)**: 按业务渠道筛选
- **产品 (Product)**: 按产品线筛选
- **标签 (Tag)**: 按策略标签筛选
- **时间范围 (Time)**: 
  - 全部时间
  - 今日活跃
  - 本周
  - 本月

#### 3.2 重置筛选

点击筛选面板中的 "Reset Filters" 按钮清除所有筛选条件。

### 四、甘特图 (Gantt Chart)

#### 4.1 查看时间轴

- 甘特图显示所有任务的时间安排
- 不同颜色表示不同任务状态
- 红色竖线表示"今日"位置

#### 4.2 调整视图

- 可以折叠/展开甘特图区域
- 调整时间刻度查看不同时间范围

### 五、AI 智能辅助

#### 5.1 自动生成任务建议

**使用条件**: 
- 必须选择 L3 策略节点
- 需要配置 AI API Key（可选）

**操作步骤**:
1. 选择 L3 策略节点
2. 点击 "AI 辅助生成" 按钮
3. AI 会自动生成 5 个具体执行任务
4. 生成的任务会自动添加到任务列表

#### 5.2 自动生成周报

**操作步骤**:
1. 选择任意策略节点
2. 点击 "智能周报" 按钮
3. AI 根据当前任务状态生成周报内容
4. 可以编辑、添加、删除汇报条目
5. 点击 "复制文本到剪贴板" 导出

**汇报类型**:
- 进展
- 问题
- 计划
- 结果
- 复盘

### 六、数据导出

#### 6.1 导出报表

**功能说明**:
- 导出当前筛选后的所有数据
- 包含 L1-L4 完整层级信息
- **Report 作为最小颗粒度**（每个 Report 一行）

**操作步骤**:
1. 应用筛选条件（可选）
2. 点击右上角 "导出报表" 按钮
3. CSV 文件自动下载

**导出内容**:
- 策略节点（L1-L3）：每个策略一行
- 任务（L4）：每个任务的每个 Report 一行
- 包含完整的层级关系和所有字段

**文件格式**:
- 文件名: `fotopro_report_YYYY-MM-DD.csv`
- 编码: UTF-8 BOM（支持中文）
- 格式: CSV（可用 Excel 打开）

### 七、用户管理（Admin 专用）

#### 7.1 添加用户

1. 以 Admin 身份登录
2. 点击右上角用户管理图标
3. 填写用户名和密码
4. 点击 "Add User" 按钮

**注意**: 
- 用户名不能重复
- 新用户默认为 "User" 角色
- 密码不能为空

#### 7.2 删除用户

1. 在用户管理面板找到要删除的用户
2. 点击删除按钮
3. 确认删除

**限制**: 不能删除当前登录的用户。

#### 7.3 查看审计日志

1. 点击右上角日志图标
2. 查看所有操作记录
3. 包括：登录、创建、更新、删除、报告生成

### 八、标签快捷选择

#### 8.1 使用快捷选择

在创建/编辑策略时：
1. 在"Tags"输入框下方会显示"快捷选择"区域
2. 显示所有已使用过的标签
3. 点击标签按钮快速添加/移除
4. 已选中的标签会高亮显示（橙色）

#### 8.2 手动输入

也可以直接在输入框中输入标签，使用逗号、分号或中文逗号分隔。

## 最佳实践

### 1. 策略规划

- **L1**: 设定年度或季度战略目标
- **L2**: 分解为区域或业务线策略
- **L3**: 细化为具体项目或行动计划

### 2. 任务管理

- 为每个 L3 策略创建 3-10 个具体任务
- 使用 AI 辅助生成初始任务列表
- 定期更新任务状态和进度
- 及时添加执行汇报，记录关键信息

### 3. 汇报管理

- **计划阶段**: 记录任务计划和预期
- **执行阶段**: 定期更新进展和遇到的问题
- **完成阶段**: 记录结果和复盘经验
- 每个任务建议至少 2-3 条汇报记录

### 4. 数据维护

- 定期导出数据备份
- 及时更新任务状态
- 保持标签使用的一致性
- 定期清理已完成的项目

## 常见问题 (FAQ)

### Q: 如何备份数据？
A: 使用"导出报表"功能导出 CSV 文件，或直接备份浏览器的 localStorage 数据。

### Q: 数据会丢失吗？
A: 数据保存在浏览器本地存储中，清除浏览器数据会丢失。建议定期导出备份。

### Q: AI 功能必须配置吗？
A: 不是必须的。不配置 AI API Key 也可以正常使用所有核心功能，只是 AI 辅助功能不可用。

### Q: 可以多人协作吗？
A: 当前版本数据存储在本地浏览器，不支持多人协作。如需协作，需要部署服务器版本。

### Q: 如何恢复误删的数据？
A: 如果浏览器数据未清除，刷新页面即可恢复。如果已清除，需要从导出的 CSV 文件重新导入（需要手动操作）。

### Q: 任务状态的含义？
A:
- **准备 (todo)**: 任务已创建，尚未开始
- **进行中 (in_progress)**: 任务正在执行
- **已完成 (completed)**: 任务已完成，待审核
- **已确认 (confirmed)**: 任务已审核确认

### Q: 优先级如何选择？
A:
- **P0**: 紧急且重要，必须优先处理
- **P1**: 重要但不紧急，尽快处理
- **P2**: 普通任务，按计划执行

## 快捷键和快捷操作

- **Enter**: 在输入框中按 Enter 键快速提交（用户管理）
- **点击标签按钮**: 快速添加/移除标签
- **拖拽调整**: 甘特图和任务列表高度可调整

## 数据说明

### 数据存储位置

所有数据存储在浏览器本地（localStorage），包括：
- 策略节点
- 任务和汇报
- 用户信息
- 审计日志

### 数据格式

- 日期格式: `YYYY-MM-DD` (ISO 8601)
- 时间戳: ISO 8601 格式
- 标签: 字符串数组，逗号分隔

### 数据导出格式

CSV 文件包含以下字段：
- Level, L1 Name, L2 Name, L3 Name
- L4 Task ID, L4 Task Content
- Task Status, Task Progress, Task Priority
- Task Owner, Task Start Date, Task End Date
- Task Product, Task Channel
- Task Score, Task Reviewer, Task Notes
- Report ID, Report Type, Report Content, Report Timestamp

## 技术支持

如遇到问题，请检查：
1. 浏览器控制台是否有错误信息
2. 数据是否正确保存到 localStorage
3. AI API Key 是否正确配置（如使用 AI 功能）

## 更新日志

### v2.5
- ✅ 添加任务审核和评分功能
- ✅ 优化数据持久化机制
- ✅ 添加标签快捷选择功能
- ✅ 优化 CSV 导出格式（Report 作为最小颗粒度）

### v2.4
- ✅ 添加用户认证系统
- ✅ 添加审计日志功能
- ✅ 支持多种 AI 模型

### v2.2
- ✅ 添加策略模态框
- ✅ 添加标签功能
- ✅ 优化甘特图显示

### v2.0
- ✅ 重构为四层架构（L1-L4）
- ✅ 添加任务汇报功能
- ✅ 添加筛选功能
