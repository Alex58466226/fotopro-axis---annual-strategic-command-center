# Gemini API 配置验证

## ✅ 配置完成后的验证步骤

### 1. 确认配置已保存

检查 `.env.local` 文件：
```bash
cat .env.local | grep GEMINI_API_KEY
```

应该看到：
```
GEMINI_API_KEY=你的真实密钥
```

### 2. 重启开发服务器

**重要**：修改 `.env.local` 后必须重启！

```bash
# 停止当前服务器（Ctrl+C）
# 然后重新启动
npm run dev
```

### 3. 测试 AI 功能

#### 方法 1：使用智能周报（推荐）

1. 打开应用：http://localhost:3000
2. 登录账号
3. 选择一个策略节点（有任务的）
4. 点击 **"智能周报"** 按钮
5. 等待几秒钟
6. 如果配置正确，应该能看到自动生成的周报内容

#### 方法 2：检查浏览器控制台

1. 打开浏览器开发者工具（F12）
2. 切换到 **Console** 标签
3. 点击"智能周报"按钮
4. 查看是否有错误信息：
   - ✅ **成功**：没有错误，能看到生成的周报
   - ❌ **失败**：看到 "未配置 AI API Key" 或 "Gemini API 错误"

### 4. 验证网络请求

1. 打开浏览器开发者工具（F12）
2. 切换到 **Network** 标签
3. 点击"智能周报"按钮
4. 查看网络请求：
   - ✅ **成功**：能看到请求发送到 `generativelanguage.googleapis.com`
   - ❌ **失败**：没有相关请求或请求失败

---

## 🎯 快速测试清单

- [ ] `.env.local` 中已配置 `GEMINI_API_KEY=你的密钥`
- [ ] 已重启开发服务器（`npm run dev`）
- [ ] 浏览器控制台没有错误
- [ ] "智能周报"功能可以正常生成内容
- [ ] 网络请求显示成功调用 Gemini API

---

## ⚠️ 如果遇到问题

### 问题 1：仍然提示"未配置 AI API Key"

**解决方法**：
1. 确认 `.env.local` 文件在项目根目录（不是 `src/` 或其他目录）
2. 确认密钥格式正确（没有多余空格、引号）
3. **必须重启开发服务器**
4. 清除浏览器缓存后重试

### 问题 2：API 调用失败

**可能原因**：
- API Key 无效或已过期
- 网络连接问题
- API 配额用尽

**解决方法**：
1. 检查 API Key 是否正确
2. 访问 https://makersuite.google.com/app/apikey 确认 Key 状态
3. 查看浏览器控制台的详细错误信息

### 问题 3：密钥格式不对

**Gemini API Key 格式**：
- 通常以 `AIzaSy` 开头
- 长度约 39 个字符
- 例如：`AIzaSyAbc123def456ghi789jkl012mno345pqr`

如果看到 `sk-` 开头的密钥，可能是 OpenAI 的 Key，不是 Gemini 的。

---

## ✅ 配置成功标志

如果看到以下情况，说明配置成功：

1. ✅ 点击"智能周报"后，几秒钟内自动生成周报内容
2. ✅ 生成的内容包含"进展"、"问题"、"计划"等类型的条目
3. ✅ 浏览器控制台没有错误信息
4. ✅ Network 标签显示成功调用 Gemini API

---

## 🚀 下一步

配置成功后，你可以：

1. ✅ 使用"智能周报"功能自动生成周报
2. ✅ 使用 AI 建议任务功能（创建任务时会自动建议）
3. ✅ 所有 AI 功能都会使用 Gemini

享受 AI 辅助的项目管理体验！
