# 项目评估看板调试指南

## 问题诊断

如果项目评估看板显示空白或所有数据为 0，请按以下步骤排查：

### 1. 检查浏览器控制台

打开浏览器开发者工具（F12），查看 Console 标签，应该能看到以下日志：

```
项目评估看板 - 数据统计: {
  activeNodeId: "...",
  activeNodeName: "...",
  activeNodeLevel: 1/2/3,
  relatedNodeIds: X,
  branchStrategies: X,
  branchTasks: X,
  totalTasks: X,
  totalStrategies: X,
  taskDetails: [...]
}
```

### 2. 检查数据情况

**如果 `branchTasks: 0`：**
- 说明当前选中的策略下没有任务
- 需要先创建 L3 策略，然后在 L3 策略下创建任务

**如果 `branchTasks > 0` 但所有指标都是 0：**
- 检查任务是否有 `progress` 值（进度）
- 检查任务是否有 `score` 值（评分）
- 检查任务是否有 `status` 值（状态）

**如果 `l3Strategies: 0`：**
- 说明当前分支下没有 L3 策略
- "事项拆分"指标需要 L3 策略才能计算

### 3. 数据要求

项目评估看板需要以下数据才能正常显示：

1. **至少 1 个任务**（用于计算进度、时间效率等）
2. **至少 1 个 L3 策略**（用于计算"事项拆分"）
3. **任务需要有进度值**（`progress: 0-100`）
4. **任务需要有状态**（`status: 'todo' | 'in_progress' | 'completed' | 'confirmed'`）
5. **任务评分**（可选，但会影响"质量"指标）

### 4. 常见问题

**问题 1：选择了 L1 策略，但显示空白**
- **原因**：L1 策略下可能没有 L3 策略或任务
- **解决**：确保 L1 → L2 → L3 → 任务 的层级结构完整

**问题 2：选择了 L3 策略，但显示空白**
- **原因**：L3 策略下可能没有任务
- **解决**：在 L3 策略下创建任务

**问题 3：有任务但所有指标都是 0**
- **原因**：任务可能没有进度值或状态
- **解决**：编辑任务，设置进度和状态

**问题 4：任务有数据但雷达图是空的**
- **原因**：可能是计算逻辑问题
- **解决**：查看控制台日志，检查 `radarData` 的值

### 5. 测试步骤

1. **创建测试数据**：
   - 创建一个 L1 策略（如"测试项目"）
   - 创建一个 L2 策略（作为 L1 的子策略）
   - 创建一个 L3 策略（作为 L2 的子策略）
   - 在 L3 策略下创建 2-3 个任务
   - 为任务设置进度（如 50%）和状态（如 'in_progress'）
   - 为任务添加评分（如 80 分）

2. **打开项目评估看板**：
   - 选择 L1 策略
   - 点击"项目评估"按钮
   - 查看控制台日志

3. **验证数据**：
   - 检查 `branchTasks` 是否 > 0
   - 检查 `l3Strategies` 是否 > 0
   - 检查任务是否有 `progress` 和 `status`

### 6. 数据匹配逻辑

项目评估看板使用以下逻辑匹配任务：

1. **直接匹配**：任务的 `parentId` 在相关节点 ID 中
2. **根节点匹配**：任务的 `rootId` 在相关节点 ID 中
3. **向上查找**：如果任务没有 `rootId`，向上查找父策略，直到找到匹配的节点

### 7. 如果仍然无法解决

请提供以下信息：
- 浏览器控制台的完整日志
- 当前选中的策略 ID 和名称
- 任务列表（包括 `parentId`、`rootId`、`progress`、`status`、`score`）
- 策略列表（包括层级关系）
